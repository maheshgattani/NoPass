# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

POST    /account/create             controllers.AccountsController.createAccount
POST    /account/login              controllers.AccountsController.login
POST    /account/password/upsert    controllers.AccountsController.upsertPassword

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
OPTIONS /                           controllers.OptionsController.rootOptions
OPTIONS /*url                       controllers.OptionsController.options(url: String)